import { redirect } from "next/navigation";

/** เส้นทางเดิมของหน้า "เรื่องที่ยืนยันแล้ว" — รวมเข้ากับหน้า /resolved แล้ว
 *
 *  ทั้งสองหน้าดึงข้อมูลชุดเดียวกันเป๊ะๆ คือ ticket ที่สถานะเป็น "completed"
 *  (listCompletedTickets() กับตัวกรองในหน้า /resolved ให้ผลเหมือนกัน) การมีสองแท็บ
 *  ที่แสดงของชุดเดียวกันทำให้คนในทีมสับสนว่าต้องดูอันไหน จึงเหลือไว้หน้าเดียว
 *  โดยเลือก /resolved เพราะมี log ครบกว่า — ไทม์ไลน์ทุกขั้น · วิธีแก้ของช่าง ·
 *  อะไหล่ที่เบิก · รูปแนบ — ตรงกับที่ขอไว้ว่าอยากได้ "log ที่สมบูรณ์"
 *
 *  ลิงก์เก่าที่เคยส่งให้กันยังใช้ได้ เพราะหน้านี้เด้งไปหน้าใหม่ให้เอง
 *  ถ้าภายหลังอยากกลับไปใช้บอร์ดเดิม: ไฟล์ CompletedBoard.tsx ยังอยู่ครบ
 *  แค่เปลี่ยนหน้านี้กลับไปเรียก <CompletedBoard /> เหมือนเดิมได้ทันที
 */
export default function CompletedPage() {
  redirect("/resolved");
}
