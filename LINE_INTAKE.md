# รับแจ้งเคสจาก LINE OA เข้า Tickets

## โครงสร้างที่ใช้งาน

LINE → Python bot (`/webhook`) → Internal API (`/api/internal/tickets`) → JSON store (`data/tickets.json`) → หน้า `/tickets`

ถ้าใช้ URL เดิม `/api/line/webhook` ของ Next.js ให้ตั้ง `LINE_BOT_WEBHOOK_URL` เพื่อส่งต่อไปยัง Python bot โดยรักษา body และลายเซ็นเดิมไว้ Docker Compose ตั้งเป็น `http://bot:8000/webhook` ให้แล้ว สำหรับรันนอก Docker ใช้ `http://localhost:8000/webhook` ใน `.env.local` โดยบอทต้องใช้ Channel secret เดียวกัน

โปรเจ็กต์มี Next.js สำหรับหน้าจัดการ, Python สำหรับบทสนทนาและ LINE API, Redis สำหรับ session/ป้องกัน webhook ซ้ำ, และ Caddy สำหรับ HTTPS แม้มี PostgreSQL ใน Compose แต่ repository ของ tickets/users/stock/equipment ปัจจุบันยังอ่านเขียน JSON ต้องรักษา volume `app_data` และ `uploads` เมื่ออัปเดตระบบ

## Logic

1. ตรวจลายเซ็น LINE ก่อนรับข้อมูล จัดลำดับ event ใน batch และล็อกการประมวลผลต่อผู้ใช้ เก็บ `webhookEventId` ที่ประมวลผลแล้ว 24 ชั่วโมงใน Redis
2. เมื่อผู้ใช้แจ้งซ่อม เบิก คืน หรือขอบริการ IT ให้เริ่มเก็บข้อมูล ไม่หยุดการเปิดเคสเพียงเพราะพบ FAQ คล้ายกัน ส่วนคำถามค้นข้อมูล/FAQ ยังตอบตาม flow เดิม
3. เก็บข้อความต้นเรื่อง แล้วถามข้อมูลที่ขาด ได้แก่ **บริษัท** สาขา ประเภท ทรัพย์สิน (ข้ามได้) รายการเบิก ชื่อ/แผนก และรายละเอียดเพิ่มเติม รูปที่ส่งระหว่าง flow ถูกแนบไปด้วย
4. เมื่อข้อมูลครบจึงสร้าง ticket สถานะ `pending` พร้อมเลขงานและตอบกลับผู้แจ้ง คำสั่งเมนู การค้นหา และสติกเกอร์ไม่สร้าง ticket เอง
5. บริษัทถูกเลือกก่อนสาขาเสมอ และบันทึกลงฟิลด์ `company` ของ ticket (ไม่ใช่ใน `meta`) หลังบ้านตรวจว่าสาขาที่ส่งมาอยู่ในบริษัทนั้นจริง ถ้าบอทไม่ส่ง `company` มาจะเดาจากชื่อสาขาให้เฉพาะเมื่อไม่กำกวม
6. รายละเอียดบันทึกทั้งข้อความต้นเรื่องและรายละเอียดเพิ่มเติม เก็บข้อมูล LINE, ชื่อ/แผนก ณ เวลาที่แจ้ง, รายการอุปกรณ์ และรูปใน `meta`
7. รหัสทรัพย์สินที่ซ้ำให้ผู้ใช้เลือกเครื่อง ส่ง `equipment_id` ไป API ด้วย หาก client รุ่นเก่าส่งเฉพาะรหัสที่กำกวม ให้เก็บรหัสดิบโดยไม่เดาผูกกับเครื่องแรก ทุกทางเข้า (พิมพ์รหัสเอง / ไล่เมนู / AI จับรหัสจากประโยคแรก) ผ่านตัวผูกเครื่องตัวเดียวกัน จึงถามเรื่องรหัสซ้ำเหมือนกันหมดและได้ `equipment_id` ติดไปกับ ticket เสมอ
8. แต่ละบทสนทนามี `request_id` เดิมตลอดการลองส่งใหม่ (สร้างตอนเปิด session และติดไปกับ session ข้าม Redis) API คืน ticket เดิมเมื่อคำขอซ้ำ จึงไม่สร้างเคสและไม่ตัดสต็อกซ้ำจากการ retry ตามปกติ ปุ่ม retry เก่าใช้ส่งข้อมูลที่ยังไม่ครบไม่ได้ — ปุ่มทำงานเฉพาะตอนที่อยู่ในสถานะ `RETRY_SUBMIT` (ส่งไปแล้วแต่หลังบ้านล่ม) นอกนั้นจะพากลับเข้าขั้นตอนเดิมแทน
9. หน้า Tickets อ่านข้อมูลใหม่ทุก 15 วินาทีเมื่อแท็บเปิดอยู่ และเมื่อกลับมาโฟกัสหน้าต่าง แสดงข้อความ รูป รายการอุปกรณ์ ช่องทาง บริษัท/สาขา และข้อมูลผู้แจ้งในรายละเอียดเคส (กรองตามบริษัทได้ที่แถบตัวกรอง)

## ลำดับขั้นตอนของบทสนทนา

```
IDLE ─▶ ASK_COMPANY ─┬─ สาขา > 11 ─▶ ASK_BRANCH_GROUP ─▶ ASK_BRANCH ─▶ ASK_TYPE ─▶ ...
                      └─ สาขา ≤ 11 ──────────────────────▶ ASK_BRANCH ─▶ ASK_TYPE ─▶ ...
```

บริษัทมี 3 ตัวเลือกคงที่ (Montipa / Motta / ส่วนกลาง) ส่วนสาขาดึงจากตาราง `branches` ที่แอดมินแก้ได้จากหน้า `/branches`

เหตุผลที่ต้องถามบริษัทก่อน: ชื่อสาขาซ้ำกันข้ามบริษัทจริง (ขอนแก่น / ชลบุรี / หาดใหญ่ มีทั้ง 2 บริษัท)
ถ้าถามแค่สาขา ticket จะบอกไม่ได้ว่าต้องส่งช่างของบริษัทไหนไป และรวมกันแล้วมีร้อยสาขาซึ่งใส่ปุ่มชุดเดียวไม่พอ

ผู้ใช้ข้ามขั้นตอนเลือกบริษัทได้ ถ้าพิมพ์ชื่อสาขาที่ไม่ซ้ำข้ามบริษัทมาตรงๆ (บอทเติมบริษัทให้เอง)
แต่ถ้าชื่อสาขานั้นมีหลายบริษัท บอทจะถามบริษัทเสมอ ไม่เดาให้

## การตั้งค่าและตรวจสอบ

- เว็บและบอทต้องตั้ง `INTERNAL_API_KEY` ตรงกัน
- บอทและ Next.js ต้องใช้ `LINE_CHANNEL_SECRET` และ `LINE_CHANNEL_ACCESS_TOKEN` ของ OA เดียวกัน
- ใช้ webhook HTTPS ที่เข้าถึงได้จริง เปิด Use webhook และ Webhook redelivery ใน LINE Developers
- URL ที่ชี้บอท `/webhook` โดยตรงยังใช้ได้ ส่วน `/api/line/webhook` ใช้การส่งต่อตามข้างต้น
- การทดสอบ Verify ของ LINE ส่ง POST ที่มี `events: []` จึงไม่สร้างเคสหรือส่งข้อความ
- หลังแก้โค้ดให้ rebuild บริการ: `docker compose up -d --build --no-deps app bot`

อ้างอิง: [LINE webhook และ redelivery](https://developers.line.biz/en/docs/messaging-api/receiving-messages/)

## ทดสอบโดยไม่ส่ง LINE จริง

```powershell
node --test tests/*.test.cjs
cd line-bot-python
.\.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

ชุด API ใช้ data directory ชั่วคราว แยกจากข้อมูลจริง ครอบคลุมทั้ง 4 ประเภท รายละเอียด รูป การส่งซ้ำ การตัดสต็อกซ้ำ รหัสทรัพย์สินซ้ำ validation การส่งต่อ webhook พร้อมลายเซ็น และชุดบริษัท/สาขา (seed ครบทุกบริษัท, ปฏิเสธสาขาข้ามบริษัท, เดาบริษัทจากชื่อสาขาเฉพาะตอนไม่กำกวม) ส่วน Python ครอบคลุมบทสนทนา การ retry การเลือกทรัพย์สิน event ซ้ำพร้อมกัน และขั้นตอนเลือกบริษัท/กลุ่ม/สาขา (รวมบริษัทที่มี 69 สาขา ซึ่งต้องแบ่งหน้าปุ่ม)

## ขอบเขตปัจจุบัน

- รับรูปภาพใน flow; ไฟล์ วิดีโอ เสียง และสติกเกอร์ยังตอบให้ส่งข้อความ/รูปตามพฤติกรรมเดิม
- สร้าง ticket เมื่อให้ข้อมูลครบ ไม่สร้าง ticket สำหรับทุกข้อความระหว่างกรอก และยังไม่ได้เพิ่มข้อความภายหลังเข้า ticket ที่ปิด flow ไปแล้วโดยอัตโนมัติ
- JSON adapter รองรับเว็บหนึ่ง process ตาม Compose ปัจจุบัน การรับประกัน transaction ข้าม tickets/stock เมื่อเครื่องดับระหว่างเขียน และการขยายหลาย replica ต้องย้าย repository ไปฐานข้อมูลที่มี transaction/unique constraint
- บอทที่ไม่ตั้ง Redis ใช้ตัวกัน event ซ้ำใน memory ซึ่งหายเมื่อ restart

## ผลตรวจวันที่ 16 กันยายน 2026

- อัปเดตบริการ Docker `app` และ `bot` แล้ว โดยคง volumes เดิม
- Python 145 tests และ API/webhook 7 tests ผ่าน รวม 152 tests
- Production build, TypeScript และ ESLint ของไฟล์ TypeScript ที่แก้ผ่าน
- เว็บและบอท health check ตอบ 200; LINE API ยืนยัน webhook active
- POST webhook ว่างพร้อมลายเซ็นจริง ผ่านทั้ง URL สาธารณะที่ OA ใช้อยู่และเส้นทางส่งต่อของ Next.js
- ตรวจ lock และ event deduplication กับ Redis ที่รันจริงผ่าน โดยลบ key ทดสอบแล้ว
- ไม่ได้ส่งข้อความทดสอบถึงผู้ใช้ LINE; การสร้างเคสทดสอบทำกับข้อมูลแยกจากระบบจริง

## ผลตรวจวันที่ 17 กันยายน 2026 (เพิ่มขั้นตอนเลือกบริษัท)

- เพิ่มบริษัท/สาขาเป็น master data จริง (`data/branches.json`) และฟิลด์ `company` ในตาราง ticket
- เพิ่มขั้นตอน `ASK_COMPANY` (และ `ASK_BRANCH_GROUP` สำหรับบริษัทที่สาขาเกิน 1 หน้าปุ่ม) ในบอท
- เติม `request_id` / `initial_message` / `equipment_id` ใน `Session` ของบอท ซึ่งเอกสารชุดนี้อธิบายไว้แล้วแต่โค้ดฝั่งบอทยังไม่มี — ผลคือก่อนหน้านี้การกดปุ่ม "ลองส่งใหม่" สร้าง ticket ซ้ำและตัดสต็อกซ้ำได้จริง
- Python 170 tests และ API/webhook 10 tests ผ่าน รวม 180 tests
- TypeScript (`tsc --noEmit`) และ ESLint ของไฟล์ที่แก้ผ่าน; `next build` รันตอน `docker compose build` อยู่แล้ว (ผ่าน `deploy.bat`)
- `deploy.bat` ตรวจของใหม่ให้ด้วย: `/health` ของบอทคืน `first_step: "ASK_COMPANY"` และ `/api/health` ของแอปคืน `features.company_branches` (จำนวนบริษัท/สาขา) — ดูจากอาการอย่างเดียวแยกไม่ออกว่า container รันโค้ดชุดไหน
- ไม่มีขั้นตอน migrate แยก: `data/branches.json` ถูก seed อัตโนมัติตอนอ่านครั้งแรก และอยู่ใน volume `app_data` เดิม
- แก้บั๊ก build ล้ม: `TicketsBoard.tsx` (client component) import ค่าคงที่บริษัทจาก `lib/db/branches.ts` ซึ่งลาก `node:fs` เข้า bundle ของเบราว์เซอร์ — ย้ายค่าคงที่ไป `lib/companies.ts` และเพิ่ม `tests/client-bundle.test.cjs` ดักไว้ (tsc/eslint จับไม่ได้ รู้ตัวตอน `next build` ล้มอย่างเดียว)
- `deploy.bat` / `backup.bat` แสดงภาษาไทยได้แล้วทั้งกรณี encoding และฟอนต์ (ดู `scripts/console-utf8.ps1`)
