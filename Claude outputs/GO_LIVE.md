# เปิดใช้งาน LINE OA จริง — เช็คลิสต์

ไล่จากบนลงล่าง ทุกข้อมี "วิธีตรวจว่าผ่าน" กำกับ อย่าติ๊กถ้ายังไม่ได้ตรวจจริง

> บทเรียนจากรอบที่แล้ว: **คำสั่งรันผ่านไม่ได้แปลว่าแก้ถูกจุด** ต้องวัดผลปลายทางเสมอ
> (เคยรัน SQL patch สำเร็จ 9 แถว แล้วพบทีหลังว่าแอปไม่ได้อ่านจาก Postgres เลย)

---

## ส่วนที่ 1 — deploy โค้ดชุดใหม่

- [ ] **1.1 build ใหม่** (ไม่ใช่แค่ restart — มีไฟล์ใหม่และ route ใหม่)
  ```powershell
  cd F:\GitHub\it-support-admin
  docker compose up -d --build app bot
  ```
  ✅ ตรวจ: `docker compose ps` ขึ้น 6 service ครบ ไม่มีตัวไหน restarting

- [ ] **1.2 route เสิร์ฟรูปทำงาน**
  ```powershell
  curl.exe -I http://127.0.0.1:3000/uploads/tickets/ไม่มีไฟล์นี้.png
  ```
  ✅ ตรวจ: ได้ **404** (ไม่ใช่ 500) — แปลว่า route ทำงานและกันไฟล์ที่ไม่มีอยู่ถูกต้อง

- [ ] **1.3 บอทเห็นคอนฟิกใหม่ และติดตั้ง rich menu สำเร็จ**
  ```powershell
  curl.exe http://127.0.0.1:8000/health
  ```
  ✅ ตรวจ: ต้องได้ JSON ที่มีครบทั้ง 4 อย่าง
  ```json
  {"ok":true,"state_backend":"redis","accept_images":true,
   "rate_limit_per_minute":20,"richmenu":"installed (richmenu-xxxx)"}
  ```
  `richmenu` เป็น `installed (...)` = เพิ่งติดตั้ง · `ok (มีอยู่แล้ว: ...)` = มีอยู่แล้วเลยข้าม — ผ่านทั้งคู่
  ถ้าเป็น `ล้มเหลว: ...` ข้อความต่อท้ายจะบอกสาเหตุตรงๆ

---

## ส่วนที่ 2 — งานที่ต้องทำด้วยมือ (Claude ทำแทนไม่ได้)

- [x] ~~**2.1 ติดตั้ง rich menu**~~ — **ไม่ต้องทำแล้ว บอทติดตั้งให้เองตอนสตาร์ท**
  ข้อ 1.1 (`docker compose up -d --build`) ติดตั้งเมนูให้เรียบร้อยแล้ว
  ✅ ตรวจ: `curl.exe http://127.0.0.1:8000/health` → ฟิลด์ `richmenu` ต้องขึ้นต้นด้วย `installed` หรือ `ok`
  ✅ ตรวจ: เปิดแชทกับ OA ในมือถือ เห็นปุ่ม 6 ปุ่มจริง (ถ้าไม่เห็น **ปิดห้องแชทแล้วเปิดใหม่ 1 ครั้ง** — LINE แคชเมนูฝั่งเครื่อง)

  ถ้า `richmenu` ขึ้น `ล้มเหลว: ...` ให้อ่านข้อความต่อท้าย แล้วยังใช้วิธีเดิมได้เสมอ:
  ```powershell
  cd F:\GitHub\it-support-admin\line-oa
  python setup_richmenu.py
  ```

- [ ] **2.2 อัปโหลดรูปโปรไฟล์ / หน้าปก / คำอธิบาย**
  https://manager.line.biz/account/@891ujhzo → ตั้งค่า → ข้อมูลบัญชี
  ไฟล์และข้อความคัดลอกได้จาก `line-oa/README.md` หัวข้อ "ขั้นที่ 2"
  ✅ ตรวจ: เปิดหน้าโปรไฟล์ OA จากมือถือ เห็นรูปและคำอธิบายครบ

- [ ] **2.3 Reissue channel secret** (secret ชุดเดิมเคยหลุดในภาพ/แชตแล้ว)
  Developers Console → Basic settings → Issue → แก้ `.env` → `docker compose up -d --force-recreate app bot`
  ✅ ตรวจ: กด **Verify** ที่ Developers Console ได้ **Success**
  ⚠️ ถ้าลืม recreate container บอทจะเงียบสนิททุกข้อความ และหาสาเหตุยากมาก

---

## ส่วนที่ 3 — ทดสอบด้วยตัวเองก่อนปล่อยให้คนอื่น

ทำครบทุกข้อจากมือถือของคุณเอง

- [ ] **3.1** สแกน QR (`qr/line-oa-qr-logo.png`) เพิ่มเพื่อน
  ✅ ได้ข้อความต้อนรับ **1 ข้อความ** (ถ้าได้ 2 แปลว่า Greeting message ยังเปิดอยู่)

- [ ] **3.2** กดปุ่ม **แจ้งซ่อม** → ✅ บอทถามสาขา

- [ ] **3.3** พิมพ์ `คีย์บอร์ดเหลือกี่อัน` → ✅ ตอบยอดคงเหลือจริง ตรงกับหน้า `/stock`

- [ ] **3.4 ส่งรูปหน้าจอเข้าไป** → ✅ ตอบ "ได้รับรูปแล้วครับ 📸" แล้วเปิด flow ถามสาขา
  *(ก่อนแก้ บอทจะเงียบสนิท)*

- [ ] **3.5** แจ้งเรื่องจนจบ → ✅ ได้เลข ticket และบรรทัด `📸 รูปแนบ: 1 รูป`

- [ ] **3.6 เปิด `/tickets` คลิก ticket นั้น** → ✅ **เห็นรูปจริงในแผงรายละเอียด** (กดแล้วเปิดเต็มได้)
  ⚠️ ข้อนี้สำคัญ — ถ้ารูปไม่ขึ้นแปลว่า route ข้อ 1.2 ไม่ทำงาน

- [ ] **3.7 กดเปลี่ยนสถานะเป็น "แก้ไขเสร็จแล้ว"** → ✅ **ได้ข้อความแจ้งเตือนเข้า LINE ภายในไม่กี่วินาที**
  *(นี่คือช่องว่างที่ใหญ่ที่สุดของระบบเดิม — แจ้งเรื่องแล้วเงียบหาย)*

- [ ] **3.8** ส่งสติกเกอร์ → ✅ ตอบว่าอ่านได้เฉพาะข้อความกับรูป (ไม่ใช่เงียบ)

- [ ] **3.9** พิมพ์รหัสทรัพย์สินที่ซ้ำ เช่น `PC2306001` → ✅ **ให้เลือกว่าเครื่องไหน** พร้อมบอกสาขา/ผู้ถือ
  *(ก่อนแก้ ระบบจะเลือกเครื่องแรกให้เงียบๆ ซึ่งอาจเป็นคนละสาขา)*

---

## ส่วนที่ 4 — ตัดสินใจก่อนเปิดให้ทั้งบริษัท

ข้อเหล่านี้ไม่ใช่บั๊ก แต่เป็นเรื่องที่ต้องตัดสินใจโดยรู้ตัว

- [ ] **4.1 โควตา push 300 ข้อความ/เดือน**
  ```powershell
  curl.exe -H "Authorization: Bearer $env:LINE_CHANNEL_ACCESS_TOKEN" https://api.line.me/v2/bot/info/quota
  ```
  ค่าตั้งต้นส่งเฉพาะ resolved/closed/cancelled = ~1 ข้อความ/ticket → รองรับ ~300 ticket/เดือน
  ถ้าคาดว่าเกิน ต้องอัปแพ็กเกจ หรือลด `LINE_NOTIFY_STATUSES` เหลือ `resolved,closed`

- [ ] **4.2 `AUTH_DISABLED=true`** — หน้าแอดมินเปิดสู่เน็ตโดยไม่ต้องล็อกอิน
  ตอนนี้ใครมี URL ก็เห็นพนักงาน 182 คน ทรัพย์สิน ค่าซ่อม และสต็อกทั้งหมด
  และยังทำให้ปุ่ม **Import Excel** (ซึ่งใช้ `xlsx` ที่มีช่องโหว่ระดับ High ยังไม่มี patch) เปิดให้คนนอกใช้ได้ด้วย
  เปิดกลับ: `AUTH_DISABLED=false` แล้ว `docker compose up -d --build app` (**build ไม่ใช่ restart**)

- [ ] **4.3 รหัสทรัพย์สินซ้ำ 28 แถว / serial ซ้ำ 7 คู่**
  ดู `db/imports/equipment-import-conflicts.csv` — บอทจะถามให้เลือกแล้ว แต่ต้นเหตุยังอยู่
  และการจับคู่ผู้ครอบครองยังอาศัยลำดับแถว ซึ่งผิดได้เมื่อรหัสซ้ำ

- [ ] **4.4 ระบบอยู่ได้ด้วยเครื่องเดียว**
  เครื่องปิด/หลับ = บอทตาย และ Tailscale Funnel ต้องรันใหม่ทุกครั้งที่รีบูต (ดู LINE_SETUP.md ข้อ 3)

- [ ] **4.5 ข้อมูลจริงอยู่ในไฟล์ JSON ไม่ใช่ Postgres**
  container `db` รัน healthy อยู่แต่ **ไม่มีโค้ดไหนใช้มันเลย** ข้อมูลจริงอยู่ที่ volume `app_data` (`/app/data/*.json`)
  ✅ ควรมี backup: `docker compose cp app:/app/data ./backup-$(Get-Date -f yyyy-MM-dd)`

---

## ส่วนที่ 5 — หลังเปิดใช้ 1 สัปดาห์ ค่อยกลับมาดู

- [ ] มีพนักงานทักเข้ามากี่คนจริง — `docker compose logs bot | Select-String "POST"`
- [ ] ticket ที่มาจากบอท (`meta.source = "line-bot"`) เทียบกับที่แอดมินคีย์เอง อัตราส่วนเท่าไร
- [ ] มีใครส่งรูปมาไหม และรูปช่วยให้ช่างเข้าใจปัญหาเร็วขึ้นจริงหรือเปล่า
- [ ] คำถามที่บอทตอบ `unknown` บ่อยๆ → เพิ่มเป็น FAQ ในระบบ
- [ ] โควตา push ใช้ไปเท่าไรแล้ว
